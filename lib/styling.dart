import 'package:flutter/material.dart';

// Primary
const COLOR_VIOLET = HSLColor.fromAHSL(1, 264, 0.55, 0.52);
const COLOR_GRAY_BLUE_DARK = HSLColor.fromAHSL(1, 217, 0.19, 0.35);
const COLOR_BLACK_BLUE_DARK = HSLColor.fromAHSL(1, 219, 0.29, 0.14);
const COLOR_WHITE = HSLColor.fromAHSL(1, 0, 0, 1);

// Neutral
const COLOR_GRAY = HSLColor.fromAHSL(1, 0, 0, 0.81);
const COLOR_GRAY_BLUE_LIGHT = HSLColor.fromAHSL(1, 210, 0.46, 0.95);

final ThemeData themeData = ThemeData(
  fontFamily: 'Barlow',
);
